/*
SELECT
    COUNT(NAME) AS `COUNT`
FROM (
    SELECT DISTINCT
        NAME
    FROM
        ANIMAL_INS
    WHERE
        NAME IS NOT NULL
) AS A
*/
SELECT
    COUNT(DISTINCT NAME) AS `COUNT`
FROM
    ANIMAL_INS
WHERE
    NAME IS NOT NULL